<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <a class="navbar-brand" href="{{ path('home', {}) }}"><img src="" /></a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor03">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="{{ path('home', {}) }}">Home <span class="sr-only">(current)</span></a>
      </li>      
      <li class="nav-item">
        <a class="nav-link" href="{{ path('home', {'category': '1'}) }}">Children</a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="{{ path('home', {'category': '2'}) }}">Fiction</a>
      </li>
    </ul>
    <div>
      <a href="{{ path('cart') }}" style="text-decoration: none; margin-right: 15px;">In Cart <span class="badge badge-primary badge-pill">{{ app.session.get('todalInCart') }}</span></a>
      <a href="#" style="text-decoration: none;"> Price <span class="badge badge-primary badge-pill">{{ app.session.get('displayPrice') }}</span></a>
    </div>
  </div>
</nav>